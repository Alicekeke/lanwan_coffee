<view class="list">
  <view class="list-title">规格 </view>
  <view class="list-menu" wx:for="{{list}}" wx:key="item">
    <view data-id="{{item.id}}" bindtap="changetype" class="item {{item.id===actived?'active':''}}">{{item.name}}</view>
  </view>
</view>